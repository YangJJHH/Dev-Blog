
이번에는 새 컴포넌트인 트리거 박스를 C++로 만들어보자

우선 오브젝트들은 서로 CollisionPreset에서 어떤 타입들과 반응할지 정할 수 있다

ex)
가고일
![[Pasted image 20260705173632.png]]

캐릭터
![[Pasted image 20260705173657.png]]

근데 둘이 보면 서로 상충되는 옵션이 있음.
가고일은 pawn을 overlap하지만, 캐릭터는 worldstatic을 block함
이런 경우 overlap을 처리됨

block되려면 둘다 block으로 설정되어있어야함

예시 이미지
![[Pasted image 20260705173919.png]]


작업
우선 문 클릭해서 해당 버튼으로 블루프린트로 새로 만들고
저 버튼의 의미는 현재 보이는 애셋을 블루프린트화 시켜서 새로 만드는거다. 어떤 기능을 추가하고싶을떄
![[Pasted image 20260705174736.png]]

그 다음 블루프린트 창에서 box collision 추가해주고
![[Pasted image 20260705174911.png]]

![[Pasted image 20260705180307.png]]
오버랩 이벤트 발생도 켜준다. 가고일도 켜져있어야함

그 다음 블루프린트 이벤트 대신, C++ 클래스로 box component를 만들어 처리할 예정
![[Pasted image 20260705180456.png]]

그러면 코드에 상위 엑터를 상속받아 만든게 아니기 때문에 아무것도 없을텐데
``` C
UCLASS()
class CRYPTRAIDER_API UTriggerComponent : public UBoxComponent
{
	GENERATED_BODY()
	
};
```

이러면 블루프린트에서 검색하면 안나오기 때문에

``` C
UCLASS( ClassGroup=(Custom), meta=(BlueprintSpawnableComponent) )
```
이렇게 추가해줘야한다

그렇게 되면 검색하면 뜬다
![[Pasted image 20260705181209.png]]
이제 우리가 만든 Trigger를 add해주자 다음에 게속..