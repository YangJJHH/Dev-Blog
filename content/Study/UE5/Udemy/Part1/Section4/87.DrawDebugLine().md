
```cpp
#include "DrawDebugHelpers.h"

FVector Start = GetComponentLocation();
FVector End = Start + GetForwardVector() * MaxGrabDistace;

DrawDebugLine(GetWorld(), Start, End, FColor::Red);
```

이건 설명 없어도 되겠지?