
# 이제 전투요소를 캐릭터에 추가해보자

우선 CombatComponent 클래스를 만들어주고,
ShooterCharacter 클래스에서
```cpp
	CombatComp = CreateDefaultSubobject<UCombatComponent>("Combat");
	CombatComp->SetIsReplicated(true);
```

이렇게 네트워크에 복제될수있도록 해준다.
컴뱃컴포넌트 생성자 내부에서 bReplicates = true 하는것과 동일함