# 무기별로 다른 설정값들을 하드코딩하지말고 데이터 에셋을 만들어 관리하자!

이거는 우리가 이전에 만든 GamePlayTag 기반으로 설정된다

우선 UDataAsset 기반으로 C++ 클래스를 만들어주자

```cpp
	UPROPERTY(EditDefaultsOnly, Category = "FPS|WeaponData|Weapons")
	TMap<FGameplayTag, FName> GripPointsMap;
```
이렇게 태그별로 무기가 어떤 소켓에 들어가는지 소켓이름을 받을 수 있도록 TMap으로 설정.


DataAsset 블루프린트를 생성해주고

![[Pasted image 20260808213038.png]]

그후  BP에서
아래처럼 Weapon 태그에 맞게 소켓을 지정해주자. 직접 Mesh에서 소켓이름 복사해 와야함

![[Pasted image 20260808213541.png]]