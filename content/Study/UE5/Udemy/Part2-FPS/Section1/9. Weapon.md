---
tags:
  - UE5
  - Relavent
  - bNetUseOwnerRelevancy
---

Weapon은 컴포넌트가 아니라 엑터 클래스 만들거다.
실제 월드에 존재하고 컴뱃컴포넌트가 이를 관리하기 위해서

우선 우리가 1인칭 메쉬,3인칭 메쉬가 있기에 Weapon 역시 여기에 대응해줘야 한다.

먼저 Weapon클래스를 엑터를 상속받아 만들고
```cpp
	UPROPERTY(EditAnywhere, Category = "Weapon")
	TObjectPtr<class USkeletalMesh> Mesh1p;
	
	UPROPERTY(EditAnywhere, Category = "Weapon")
	TObjectPtr<class USkeletalMesh> Mesh3p;
```

2가지 메쉬를 선언해주고

```cpp
AWeapon::AWeapon()
{
 	// Set this actor to call Tick() every frame.  You can turn this off to improve performance if you don't need it.
	PrimaryActorTick.bCanEverTick = false;
	bReplicates = true;
	bNetUseOwnerRelevancy = true;
	
	Mesh1p = CreateDefaultSubobject<USkeletalMeshComponent>(TEXT("Mesh1p"));
	Mesh1p->VisibilityBasedAnimTickOption = EVisibilityBasedAnimTickOption::OnlyTickPoseWhenRendered;
	Mesh1p->bReceivesDecals = false;
	Mesh1p->CastShadow = false;
	Mesh1p->SetHiddenInGame(true);
	SetRootComponent(Mesh1p);

	Mesh3p = CreateDefaultSubobject<USkeletalMeshComponent>(TEXT("Mesh3p"));
	Mesh3p->VisibilityBasedAnimTickOption = EVisibilityBasedAnimTickOption::OnlyTickPoseWhenRendered;
	Mesh3p->bReceivesDecals = false;
	Mesh3p->CastShadow = true;
	Mesh3p->SetupAttachment(Mesh1p);
	SetRootComponent(Mesh3p);
}
```

캐릭터의 1인칭,3인칭 메쉬와 비슷하게 설정해준다

bReplicates = true; 기준은 
"이 객체가 다른 컴퓨터에 존재해야 하는가" 이다

### bNetUseOwnerRelevancy, Relavant
bNetUseOwnerRelevancy 이거는 뭐냐?

한마디로:
> **"이 Actor의 네트워크 중요도(Relevancy)를 Owner를 기준으로 판단해라."**

Relevent 개념

![[Pasted image 20260808171032.png]]

이런 개념
거리가 매우 먼 C클라한테는 A,B의 무기 정보가까지 복제할 필요가 없음.
그래서 나중에 Relavent해지면 그때서야 서버가 동기화 해줌

그래서 총같은경우는 캐릭터의 종속적 즉 캐릭터의 소유니깐 Owner의 Relevancy를 따라간다


그럼 이제 해당 클래스기반의 BP_Weapon을 만들어주자

![[Pasted image 20260808171645.png]]

이건 이제 권총이나 , 소총의 기본 베이스 BP가 될거다
그래서 이 BP기반으로 또 Pistol,Rifle을 만든다

![[Pasted image 20260808171804.png]]

먼저 Rifle을 열어 1p,3p 모두 동일한 메쉬로 설정

![[Pasted image 20260808172019.png]]


그 다음 이제 캐릭터의 어떤 본 즉 어떤 소켓에 달지 확인해보자
캐릭터의 SKM을 열고 본을 확인하면

![[Pasted image 20260808172613.png]]

이렇게 총을 위한 소켓들이 있다. 
여기서 해당 소켓을 우클릭하고  아래처럼 해주면 미리볼수있음

![[Pasted image 20260808173646.png]]

![[Pasted image 20260808173732.png]]


