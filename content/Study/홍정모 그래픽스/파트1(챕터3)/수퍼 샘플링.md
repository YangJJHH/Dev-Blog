---
tags:
  - Part1
---

기존 샘플링 방법은 아래 그림과 같이 눈에서 픽셀로 통과하는 레이를 하나만 쏴서 색을 결정했다
![[Pasted image 20240812134916.png]]

하지만 수퍼샘플링은 레이를 하나만 쏘는게 아니라 픽셀 하나에서 여러개의 레이를 쏴서 평균을 내어 부드럽게 적용하는 것이다.
![[Pasted image 20240812134953.png]]

![[Pasted image 20240812143306.png]]

```cpp
vec3 traceRay2x2(vec3 eyePos, vec3 pixelPos, const float dx, const int recursiveLevel)
		{
			if (recursiveLevel == 0)
			{
				Ray myRay{pixelPos, glm::normalize(pixelPos - eyePos)};
				return traceRay(myRay);
			}

			const float subdx = 0.5f * dx;

			vec3 pixelColor(0.0f);
			pixelPos = vec3(pixelPos.x - subdx * 0.5f, pixelPos.y - subdx * 0.5f, pixelPos.z);
			//[수정] 강의 영상과 달리 subdx에 0.5f를 곱해줬습니다.
			for (int j = 0; j < 2; ++j)
			{
				for (int i = 0; i < 2; ++i)
				{
					vec3 subPos(pixelPos.x + subdx * float(i), pixelPos.y + subdx * float(j), pixelPos.z);
					pixelColor += traceRay2x2(eyePos, subPos, subdx, recursiveLevel - 1);
				}
			}
			

			return pixelColor * 0.25f;
		}
```

수퍼샘플링 전
![[Pasted image 20240812143415.png]]

수퍼샘플링 후
![[Pasted image 20240812143853.png]]

계단 현상이 조금 완화된다. 하지만 리얼타임 그래픽스에선 수퍼샘플링은 너무 느리기 때문에 잘 사용하지 않는다.