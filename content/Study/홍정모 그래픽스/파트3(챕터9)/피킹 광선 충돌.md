---
tags:
  - Part3
---

원리
![[Pasted image 20241008170237.png]]

![[Pasted image 20241008170405.png]]
카메라를 시작점으로 레이를 쏘게되면 Near플레인 전에 있는 물체 즉 랜더링되지 않는 물체도 검출될수 있으니 시작점은 near 끝점은 far로 해서 레이를 쏜다

전에 레이트레이싱을 공부할때는 구충돌을 찾기 위해서 직접 구현했지만 이번에는 DirectX에서 구현된 기능을 사용해본다
```cpp
BoundingSphere m_mainBoundingSphere;
```


Update에서 실행되는 코드이다
전체코드
```cpp
    // 마우스 클릭했을 때만 업데이트
    if (m_leftButton) {

        // OnMouseMove()에서 m_cursorNdcX, m_cursorNdcY 저장

        // ViewFrustum에서 가까운 면 위의 커서 위치 (z값 주의)
        Vector3 cursorNdcNear = Vector3(m_cursorNdcX, m_cursorNdcY, 0.0f);

        // ViewFrustum에서 먼 면 위의 커서 위치 (z값 주의)
        Vector3 cursorNdcFar = Vector3(m_cursorNdcX, m_cursorNdcY, 1.0f);

        // NDC 커서 위치를 월드 좌표계로 역변환 해주는 행렬
        Matrix inverseProjView = (viewRow * projRow).Invert();

        Vector3 cursorWorldNear = Vector3::Transform(cursorNdcNear, inverseProjView);
        Vector3 cursorWolrdFar = Vector3::Transform(cursorNdcFar, inverseProjView);

        // ViewFrustum 안에서 PickingRay의 방향 구하기
        Vector3 dir = cursorWolrdFar - cursorWorldNear;
        dir.Normalize();

        // 광선을 만들고 충돌 감지
        SimpleMath::Ray curRay = SimpleMath::Ray(cursorWorldNear, dir);
        float dist = 0.0f;
        m_selected = curRay.Intersects(m_mainBoundingSphere, dist);

        if (m_selected) {
            m_cursorSphere.m_basicPixelConstantData.eyeWorld = eyeWorld;

            // 충돌 지점에 작은 구 그리기
            Matrix modelMat = Matrix::CreateTranslation(cursorWorldNear + dist*dir); // TODO:

            Matrix invTransposeRow = modelMat;
            invTransposeRow.Translation(Vector3(0.0f));
            invTransposeRow = invTransposeRow.Invert().Transpose();
            m_cursorSphere.m_basicVertexConstantData.model =
                modelMat.Transpose();
            m_cursorSphere.m_basicVertexConstantData.invTranspose =
                invTransposeRow.Transpose();
            m_cursorSphere.m_basicVertexConstantData.view = viewRow.Transpose();
            m_cursorSphere.m_basicVertexConstantData.projection =
                projRow.Transpose();
            m_cursorSphere.UpdateConstantBuffers(m_device, m_context);
        }
    }
```

코드 별 분석
```cpp
Vector3 cursorNdcNear = Vector3(m_cursorNdcX, m_cursorNdcY, 0.0f);

        // ViewFrustum에서 먼 면 위의 커서 위치 (z값 주의)
Vector3 cursorNdcFar = Vector3(m_cursorNdcX, m_cursorNdcY, 1.0f);
```

우선 마우스의 커서위치를 NDC좌표로 변환한 위치로 얻어온다.
해당 위치는 투영되었을때의 위치이므로 z값에 0,1을 넣어 Near,Far 평면상의 위치를 얻어온다. (여기까지는 NDC좌표계임)

그 다음 해당 near,far 상의 위치를 월드상으로 좌표로 변환해줘야 한다. 월드상의 ray를 쏴서 충돌검사를 해야하기 때문.
```cpp
// NDC 커서 위치를 월드 좌표계로 역변환 해주는 행렬
Matrix inverseProjView = (viewRow * projRow).Invert();

Vector3 cursorWorldNear = Vector3::Transform(cursorNdcNear, inverseProjView);
Vector3 cursorWolrdFar = Vector3::Transform(cursorNdcFar, inverseProjView);
```
NDC좌표가 view,projection까지 진행된 결과이기 때문에 해당행렬의 역행렬을 구해서 곱해준다. 그 다음 NDC상의 near,far평면상의 좌표에 해당 행렬을 구하여 World상의 좌표로 변환한다. 
(이거는 view,proj의 역행렬을 곱해줘도 되지만 UnProject라는 Directx내장함수써도 똑같음. 단 그때는 좌표를 NDC좌표가 아니라 스크린상 좌표를 보내줘야함. 내부적으로 NDC변환을 하기 때문)

```cpp
// ViewFrustum 안에서 PickingRay의 방향 구하기
Vector3 dir = cursorWolrdFar - cursorWorldNear;
dir.Normalize();
```
월드상 near부터 far평면까지의 방향벡터 구하기


```cpp
// 광선을 만들고 충돌 감지
SimpleMath::Ray curRay = SimpleMath::Ray(cursorWorldNear, dir);
float dist = 0.0f;
m_selected = curRay.Intersects(m_mainBoundingSphere, dist);
```
방향벡터로 Ray를 쏘고 구와 충돌하는지 Intersects함수를 통해 구함
dist에는 충돌지점까지의 거리,m_selected에는 충돌여부가 저장됨

m_mainBoundingSphere 해당 boundingSphere는 아래처럼 정의함
```cpp
// 동일한 크기와 위치에 BoundingSphere 만들기
m_mainBoundingSphere = BoundingSphere(center, radius);
```
물체와 동일한 위치와 크기의 바운딩 범위 지정하기

그 다음 BoundingSphere에 ray가 충돌됐다 판정되면 충돌위치에 작은구를 생성해준다
```cpp
if (m_selected) {
            m_cursorSphere.m_basicPixelConstantData.eyeWorld = eyeWorld;

            // 충돌 지점에 작은 구 그리기
            Matrix modelMat = Matrix::CreateTranslation(cursorWorldNear + dist*dir); // TODO:

            Matrix invTransposeRow = modelMat;
            invTransposeRow.Translation(Vector3(0.0f));
            invTransposeRow = invTransposeRow.Invert().Transpose();
            m_cursorSphere.m_basicVertexConstantData.model =
                modelMat.Transpose();
            m_cursorSphere.m_basicVertexConstantData.invTranspose =
                invTransposeRow.Transpose();
            m_cursorSphere.m_basicVertexConstantData.view = viewRow.Transpose();
            m_cursorSphere.m_basicVertexConstantData.projection =
                projRow.Transpose();
            m_cursorSphere.UpdateConstantBuffers(m_device, m_context);
        }
```
다른 물체 그리는것과 동일 
Matrix::CreateTranslation(cursorWorldNear + dist\*dir); 이 부분만 보면되는데
레이가 충돌한지점의 위치를 구해서 거기로 중심을 이동시켜줌


## Render
```cpp
    if (m_leftButton && m_selected)
        m_cursorSphere.Render(m_context);
```
마우스가 눌렸고, 커서에 선택된 물체가 검출됐다면 작은 구를 그린다.

![[Pasted image 20241008200734.png]]