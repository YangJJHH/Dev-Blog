---
tags:
  - Part3
---

우선 이게 기존에 사용하던 회전 즉, 오일러 각도를 이용한 회전이다.
```cpp
    // Euler Angles
    m_mainSphere.UpdateModelWorld(Matrix::CreateRotationX(m_eulerAlpha) *
                                  Matrix::CreateRotationY(m_eulerBeta) *
                                  Matrix::CreateRotationZ(m_eulerGamma) *
                                  Matrix::CreateTranslation(translation));
```

해당 회전을 쿼터니언으로 바꿔보자
```cpp
// TODO: Euler Angles 대신에 Quaternion을 이용해서 회전 구현하기
    // - Quaternion(const Vector3& v, float scalar) 생성자
    Vector3 n = Vector3(m_quatX, m_quatY, m_quatZ);
    n.Normalize();
    Quaternion q(n * sin(m_quatTheta * 0.5f), cos(m_quatTheta * 0.5f));
    
    m_mainSphere.UpdateModelWorld(Matrix::CreateFromQuaternion(q) *
                                  Matrix::CreateTranslation(translation));
```
![[Pasted image 20241008215626.png]]
쿼터니언 생성자에 들어가는 식은 위의 식이다.



더 직관적인 이방법도 있음
```cpp
Quaternion q = Quaternion::CreateFromAxisAngle(n,m_quatTheta);
```

이미지로 보이지 않지만 짐벌락 현상은 나타나지 않는다.
![[Pasted image 20241008215733.png]]