---
tags:
  - Part3
---
일단 2가지 방법이 있다
1. 레이트레이싱에서 썼던 방법으로 마우스 커서 방향으로 레이를 쏴서 삼각형 하나하나 체크하는 것
2. ㅁㄴㅇ

우선 1번은 일반적으로 하기엔 성능이 안나온다, 특별한 자료구조를 써야 한다고 하는데 파트4에서 나온다함


## 화면캡쳐
```cpp
// 화면 캡쳐
if (m_captureFlag) {

	m_captureFlag = false;

	ComPtr<ID3D11Texture2D> backBuffer;
	m_swapChain->GetBuffer(0,
						   IID_PPV_ARGS(backBuffer.GetAddressOf()));
	m_context->ResolveSubresource(m_tempTexture.Get(), 0,
								  backBuffer.Get(), 0,
								  DXGI_FORMAT_R8G8B8A8_UNORM);

	D3D11Utils::WriteToFile(m_device, m_context, m_tempTexture,
							"captured.png");
}
```
여기서는 멀티샘플링해서 결과가 저정이되어있는 Texture2DMS를 우리가 일반적으로 사용하는 Texture2D로 변환하는 과정이다.

m_swapChain->GetBuffer
	 스왑체인의 랜더링결과가 들어있는 백버퍼 포인터를 가져온다
m_context->ResolveSubresource
	 백버퍼의 포인터를 통해 m_tempTexture에 복사해준다.
WriteToFile
	 GPU에있는 버퍼 데이터를 CPU로 옮겨 저장하는 작업

그럼 WriteToFile 함수를 봐보자
```cpp
void D3D11Utils::WriteToFile(ComPtr<ID3D11Device> &device,
                             ComPtr<ID3D11DeviceContext> &context,
                             ComPtr<ID3D11Texture2D> &textureToWrite,
                             const std::string filename) {

    D3D11_TEXTURE2D_DESC desc;
    textureToWrite->GetDesc(&desc);
    desc.SampleDesc.Count = 1;
    desc.SampleDesc.Quality = 0;
    desc.BindFlags = 0;
    desc.MiscFlags = 0;
    desc.CPUAccessFlags = D3D11_CPU_ACCESS_READ; // CPU에서 읽기 가능
    desc.Usage = D3D11_USAGE_STAGING; // GPU에서 CPU로 보낼 데이터를 임시 보관

    ComPtr<ID3D11Texture2D> stagingTexture;
    if (FAILED(device->CreateTexture2D(&desc, nullptr,
                                       stagingTexture.GetAddressOf()))) {
        cout << "Failed()" << endl;
    }

    // 참고: 전체 복사할 때
    // context->CopyResource(stagingTexture.Get(), pTemp.Get());

    // 일부만 복사할 때 사용
    D3D11_BOX box;
    box.left = 0;
    box.right = desc.Width;
    box.top = 0;
    box.bottom = desc.Height;
    box.front = 0;
    box.back = 1;
    context->CopySubresourceRegion(stagingTexture.Get(), 0, 0, 0, 0,
                                   textureToWrite.Get(), 0, &box);

    // R8G8B8A8 이라고 가정
    std::vector<uint8_t> pixels(desc.Width * desc.Height * 4);

    D3D11_MAPPED_SUBRESOURCE ms;
    context->Map(stagingTexture.Get(), NULL, D3D11_MAP_READ, NULL,
                 &ms); // D3D11_MAP_READ 주의

    // 텍스춰가 작을 경우에는
    // ms.RowPitch가 width * sizeof(uint8_t) * 4보다 클 수도 있어서
    // for문으로 가로줄 하나씩 복사
    uint8_t *pData = (uint8_t *)ms.pData;
    for (unsigned int h = 0; h < desc.Height; h++) {
        memcpy(&pixels[h * desc.Width * 4], &pData[h * ms.RowPitch],
               desc.Width * sizeof(uint8_t) * 4);
    }

    context->Unmap(stagingTexture.Get(), NULL);

    stbi_write_png(filename.c_str(), desc.Width, desc.Height, 4, pixels.data(),
                   desc.Width * 4);

    cout << filename << endl;
}
```

우선 ID3D11Texture2D stagingTexture를 만드는 D3D11_TEXTURE2D_DESC의 옵션을 보면 CPU에서 읽고 전송 가능하게 아래 옵션으로 생성된다.
D3D11_CPU_ACCESS_READ
D3D11_USAGE_STAGING

그 후 복사할 데이터 영역을 박스로 잡아서 일부만 stagingTexture에 복사해준다
```cpp
context->CopySubresourceRegion(stagingTexture.Get(), 0, 0, 0, 0,
                                   textureToWrite.Get(), 0, &box);
```


그 다음 이 부분을 보면 Constant버퍼를 gpu로 전송시켜주는 것과 동일하게 map,unmap을 사용한다
```cpp
// R8G8B8A8 이라고 가정
    std::vector<uint8_t> pixels(desc.Width * desc.Height * 4);

    D3D11_MAPPED_SUBRESOURCE ms;
    context->Map(stagingTexture.Get(), NULL, D3D11_MAP_READ, NULL,
                 &ms); // D3D11_MAP_READ 주의

    // 텍스춰가 작을 경우에는
    // ms.RowPitch가 width * sizeof(uint8_t) * 4보다 클 수도 있어서
    // for문으로 가로줄 하나씩 복사
    uint8_t *pData = (uint8_t *)ms.pData;
    for (unsigned int h = 0; h < desc.Height; h++) {
        memcpy(&pixels[h * desc.Width * 4], &pData[h * ms.RowPitch],
               desc.Width * sizeof(uint8_t) * 4);
    }

    context->Unmap(stagingTexture.Get(), NULL);
```
근데 여기서는 GPU => CPU로 전송하는 것이기 떄문에 즉 GPU의 데이터를 읽기 만 하기 때문에 D3D11_MAP_READ로 해주고 가로줄 한줄씩 memcpy로 cpu메모리에 복사한다.



우리가 물체만을 특정한 색으로 지정하고 나머지 부분을 저렇게 랜더링하고 싶다면.
```cpp
struct PixelShaderOutput
{
    float4 pixelColor : SV_Target0;
    float4 indexColor : SV_Target1;
};
```
이렇게 픽셀쉐이더 아웃풋 부분을 구조체로 만들어 랜더타겟마다 다른 색상을 지정해주면 된다.
```cpp
    // Multiple render targets
    // 인덱스를 저장할 RenderTarget을 추가
    ID3D11RenderTargetView *targets[] = {m_renderTargetView.Get(),
                                         m_indexRenderTargetView.Get()};
    m_context->OMSetRenderTargets(2, targets, m_depthStencilView.Get());
    m_context->OMSetDepthStencilState(m_depthStencilState.Get(), 0);
```
랜더 타겟도 이렇게 한개가 아니라 두개의 배열로 지정하면 각각 랜더타겟에 다른 결과물을 담아낼수 있음, 첫번째로 전달하는 랜더타겟뷰가 우리가 화면에 보이는 랜더타겟
![[Pasted image 20241008134741.png]]

이 원리를 이용해 랜더링용 랜더타겟에는 원래 쉐이딩대로 색깔을 지정하고 다른 랜더타겟에 저렇게 물체만을 구분하게 색을 지정한다음. 캡처를 해서 cpu에서 마우스 커서 위치가 물체의 색인지 구별해서 구현할수있음.



## 피킹구현

아래코드는 Render 맨마지막 부분에 추가된다.
m_indexTexture는 m_indexRenderTargetView가 랜더하는 버퍼이다.
따라서 위에 그림처럼 물체만 랜더링된 이미지가 들어오게되는 버퍼.
```cpp
    // Picking
    {
        // TODO: m_pickColor 업데이트
        //  GPU->CPU는 화면 캡쳐 코드 참고
        m_context->ResolveSubresource(m_indexTempTexture.Get(), 0,
                                      m_indexTexture.Get(), 0,
                                      DXGI_FORMAT_R8G8B8A8_UNORM);
        D3D11_BOX box;
        box.left = m_cursorX;
        box.right = m_cursorX + 1;
        box.top = m_cursorY;
        box.bottom = m_cursorY + 1;
        box.front = 0;
        box.back = 1;
        m_context->CopySubresourceRegion(m_indexStagingTexture.Get(), 0, 0, 0,
                                         0, m_indexTempTexture.Get(), 0, &box);

        D3D11_MAPPED_SUBRESOURCE ms;
        m_context->Map(m_indexStagingTexture.Get(), NULL, D3D11_MAP_READ, NULL,
                       &ms);
        memcpy(m_pickColor, ms.pData, sizeof(uint8_t) * 4);
        m_context->Unmap(m_indexStagingTexture.Get(),NULL);
    }
```

- ResolveSubresource
	GPU에 랜더링된 이미지는 Texture2DMS 우리가 쓸 형식은 Texture2D 이 형식을 변환하는과정
- CopySubresourceRegion
	지정한 box영역의 크기의 버퍼를 복사하는 과정. 참고로 m_indexStagingTexture는 1x1크기로 생성함, 마우스가 가르키는 픽셀 하나만 가져올것이기 때문


다음 프레임 업데이트에서 
m_pickColor 가 자기 indexColor랑 동일하면 색을 바꾸어주고 아니면 회색으로 처리한다
```cpp
// TODO:
    if (m_pickColor[0] == 255) {
        m_meshGroupSphere.m_basicPixelConstantData.material.diffuse =
            Vector3(1.0f, 0.1f, 0.1f);
    } else {
        m_meshGroupSphere.m_basicPixelConstantData.material.diffuse =
            Vector3(0.5f);
    }
```

당연히 물체 초기화 부분에 indexColor지정해줘야함
```cpp
	m_meshGroupSphere.m_basicPixelConstantData.indexColor =
		Vector4(1.0f, 0.0f, 0.0f, 0.0f);
	m_meshGroupSphere.UpdateConstantBuffers(m_device, m_context);
```




![[Pasted image 20241008165758.png]]